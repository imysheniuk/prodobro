!!!
%html{:lang => "en"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}
    %meta{:charset => "utf-8"}
    %link{:href => "https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css", :rel => "stylesheet"}
    :css
      a
      {
      color: #17a2b8
      }
      img
      {
      max-width:350px;
      width: 100%;
      height:200px;
      border-radius:5px
      }
      .container 
      {
      background-color: #f7f7f7;
      }
      .news
      {
      margin-top: 20px;
      margin-left: 20px;
      max-width:350px;
      }
      .title h3
      {
      display:inline;
      }
      .title
      {
      margin-top:15px;
      margin-bottom:5px;
      }
      span
      {
      padding-top:15px;
      float:right;
      }
      p
      {
      overflow: hidden;
      text-overflow: ellipsis;
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      }
      .paginate
      {
      margin: 20px 0 0 20px
      }
      #create_news
      {
      margin: 20px 0 0 20px
      }
  %body
    .container
      %a= link_to 'Create news', new_news_path, class: 'btn btn-success', id: 'create_news'
      .row
        - @news.each do |n|
          .col-md-4.news
            %img.img-rounded{src: "https://www.w3schools.com/w3images/lights.jpg"}
            %div
              .title
                %h3= link_to n.title, news_path(n)
              .body
                %p= n.description
                %a= link_to 'Edit', edit_news_path(n), class: 'btn btn-outline-info'
                %a= link_to "Delete news", n, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-outline-info'
                %span= n.created_at.strftime("%d %b, %Y")
      .paginate
        = paginate @news         